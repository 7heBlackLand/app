name: Build and Commit Android APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout your repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Set up Python 3.10
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 3️⃣ Install system dependencies (Ubuntu 24.04 compatible)
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            openjdk-17-jdk \
            zip \
            unzip \
            libffi-dev \
            libssl-dev \
            git \
            pkg-config \
            libgl1-mesa-dev \
            libgles2-mesa-dev \
            libncurses6 \
            python3-pip

      # 4️⃣ Cache build directories for faster builds
      - name: Cache Buildozer and Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.buildozer
            ~/.gradle
          key: buildozer-${{ runner.os }}-${{ hashFiles('buildozer.spec') }}
          restore-keys: |
            buildozer-${{ runner.os }}-

      # 5️⃣ Install global Python tools
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install buildozer Cython kivy virtualenv

      # 6️⃣ Prepare Buildozer environment
      - name: Prepare Buildozer environment
        run: |
          mkdir -p ~/.buildozer
          python -m venv ~/.buildozer/venv
          source ~/.buildozer/venv/bin/activate
          pip install --upgrade pip
          pip install buildozer Cython kivy

      # 7️⃣ Show debug info
      - name: Verify environment
        run: |
          source ~/.buildozer/venv/bin/activate
          echo "Python version:"
          python --version
          echo "Installed packages:"
          pip list
          echo "===== buildozer.spec ====="
          cat buildozer.spec || echo "buildozer.spec NOT FOUND!"
          echo "=========================="

      # 8️⃣ Build the APK
      - name: Build APK
        run: |
          source ~/.buildozer/venv/bin/activate
          buildozer android debug

      # 9️⃣ Show output directory
      - name: List built files
        run: |
          echo "===== bin directory ====="
          ls -lh bin || echo "bin/ directory not found!"
          echo "=========================="

      # 🔟 Upload built APK as artifact
      - name: Upload built APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: built-apk
          path: bin/*.apk

      # 1️⃣1️⃣ Commit built APK to repo (optional)
      - name: Commit built APK to repository
        if: success()
        run: |
          mkdir -p output_apk
          cp bin/*.apk output_apk/ || echo "No APK found"
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add output_apk/ || true
          git diff --quiet && echo "No changes to commit" || git commit -m "Add latest built APK"
          git push
